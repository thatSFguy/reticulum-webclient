{
  "name": "reticulum-webclient",
  "version": "0.33.5",
  "description": "Browser-based Reticulum messaging client — encrypted LXMF chat and NomadNet browsing over an RNode LoRa modem (Web Bluetooth/Web Serial), rnsd via WebSocket/TCP, or an agnostic-LoRa-Net BLE mesh.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node tests/aln.mjs && node tests/reply.mjs && node tests/roundtrip.mjs && python tests/run_tests.py"
  },
  "devDependencies": {
    "@msgpack/msgpack": "3.0.0-beta2",
    "@noble/curves": "1.8.1",
    "esbuild": "^0.28.0",
    "leaflet": "^1.9.4"
  }
}
